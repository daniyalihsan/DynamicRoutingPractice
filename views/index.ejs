<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main width-full text-white min-h-screen bg-zinc-900">
      <div class="p-10 text-white">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 outline-none w-full rounded-md bg-zinc-800"
            type="text"
            name="title"
            placeholder="Enter role"
          />
          <textarea
            class="block mt-3 outline-none w-full rounded-md bg-zinc-800 resize-none px-5 py-2 by-zinc-800"
            name="details"
            placeholder="Input task details here"
          ></textarea>
          <input
            class="mt-3 bg-blue-600 px-5 py-3 rounded-md"
            type="submit"
            value="Create Task"
          />
          <input
            class="mt-3 bg-blue-600 px-5 py-3 rounded-md"
            type="submit"
            value="Delete Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-4 flex-wrap">
        <!--To dynamically read any file sent from backend-->
        <% if (files.length > 0) { %> <% files.forEach((file) => { %>
        <div class="task w-72 px-3 py-2 bg-zinc-800 rounded-md">
          <h1 class="text-white text-3xl tracking-tighter"><%=file%></h1>
          <div class=" flex justify-between">
          <a href="http://localhost:8080/file/<%= file %>" class="text-blue-500">Read more.</a>
          <a href="http://localhost:8080/edit/<%= file %>" class="text-zinc-600">edit.</a>
          </div>
          <!-- Form to delete the file -->
        <form action="/delete" method="POST">
            <!-- Hidden field to simulate a DELETE request -->
            <input type="hidden" name="_method" value="DELETE">
            <!-- Pass the file name to the backend -->
            <input type="hidden" name="title" value="<%= file %>">
            <button type="submit" class="text-red-500">Delete</button>
          </form>
        </div>
        <% }) %> <% } else { %>
        <h1 class="text-zinc-600">No task here.</h1>
        <% } %>
      </div>
    </div>
    <script src="javascripts/script.js"></script>
  </body>
</html>
